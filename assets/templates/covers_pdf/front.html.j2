<!DOCTYPE html>
<html>
<head>
  <style>
    body {
      font-family: sans-serif;
      text-align: center;
      padding-top: 200px;
    }
    h1 {
      font-size: 48px;
      margin-bottom: 20px;
    }
    p {
      font-size: 20px;
      color: #555;
    }
  </style>
</head>
<body>
  <h1>{{ config.site_name }}</h1>
  <p>{{ config.site_description }}</p>
  <p>Generat el {{ now.strftime('%d/%m/%Y') }}</p>
</body>
</html>